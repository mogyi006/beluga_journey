### Setup
To run the notebooks in this module on Azure ML, you can use the following setup:
1. Create a new Azure ML Compute Instance or use an existing one.
2. Clone the `beluga_journey` repository to your Compute Instance.
3. Install the required dependencies by running `python -mpip install -r requirements.txt` in the terminal.
4. Open the notebooks and run them.